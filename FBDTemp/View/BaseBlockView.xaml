<UserControl x:Class="FBDTemp.View.BaseBlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="100" Background="Green">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" ItemsSource="{Binding Path=Input}" DisplayMemberPath="Input"  ></ListBox>
        <Label Grid.Column="1" Content="{Binding Name}"/>
        <ListBox Grid.Column="2" ItemsSource="{Binding Path=Output}" DisplayMemberPath="Output"  ></ListBox>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="AddInput" Command="{Binding Path=AddInputCommand}" />
                <MenuItem Header="RemoveInput" Command="{Binding Path=RemoveInputCommand}" />
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
   
</UserControl>
